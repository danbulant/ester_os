
//To-Do: verify user with API
function verifyUser(name, password){
    return new Promise((resolve, reject) => {
        if(password = "1234")resolve(true);
        resolve(false);
    })
}

export default verifyUser;